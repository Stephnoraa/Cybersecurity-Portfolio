# Fundamental Security Operations: Linux Permissions & SQL Auditing
**Core Focus:** Systems Security Operations
## Tools & Technologies: 
- Linux Bash
- chmod, ls
- PostgreSQL/MySQL syntax.
## Key Scope:
- Reconfigured file/directory ownership and permissions (ls -la, chmod) across system directories to restrict group/other access.
- Authored filtered SQL queries (WHERE, AND, OR, NOT, LIKE) on authentication logs to extract after-hours failed sign-ins and filter department machine data.
